const firstStart = 0;

currentResult = firstStart + 10 * 3;

let calculationDescription = `(${firstStart} + 10) * 3`;
outputResult(currentResult, calculationDescription);

function add(num1, num2) {
  const result = num1 + num2;
  alert('THe Result is : ' + result);
}

add(1,2)
